# Python 3.8 — all versions below have pre-built wheels for cp38-win_amd64

# Experiment configs (YAML)
pyyaml>=6.0.1,<7.0.0

# Core scientific stack
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<2.2.0
scipy>=1.10.0,<1.12.0
scikit-learn>=1.3.0,<1.4.0

# PostgreSQL driver (pinned — 2.9.10 dropped the py38 wheel)
psycopg2-binary==2.9.9

# WRDS database access (install with: pip install wrds --no-deps)
wrds>=3.1.0

# Neural networks
torch>=2.0.0,<2.5.0

# Dashboard
streamlit>=1.28.0,<1.41.0
plotly>=5.15.0

# Data storage
pyarrow>=12.0.0,<18.0.0

# Progress bars
tqdm>=4.65.0

# Statistics
statsmodels>=0.14.0,<0.15.0

# Excel support (for Goyal macro data)
openpyxl>=3.1.0
xlrd>=2.0.1
